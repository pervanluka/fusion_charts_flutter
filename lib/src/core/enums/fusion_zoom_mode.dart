/// Which axes can be zoomed.
enum FusionZoomMode {
  /// Only X-axis can be zoomed.
  x,

  /// Only Y-axis can be zoomed.
  y,

  /// Both axes can be zoomed (default).
  both,

  /// No zooming (locked).
  none,
}
