/// Position of a label badge relative to a reference line.
enum FusionLabelPosition {
  /// Label aligned to the left edge of the chart area.
  left,

  /// Label aligned to the right edge of the chart area.
  right,

  /// Label above the line, left-aligned.
  topLeft,

  /// Label above the line, right-aligned.
  topRight,
}
