enum MarkerShape {
  /// ● Circular marker
  circle,

  /// ■ Square marker
  square,

  /// ▲ Triangle marker (pointing up)
  triangle,

  /// ◆ Diamond marker (rotated square)
  diamond,

  /// + Cross marker (plus sign)
  cross,

  /// × X-shaped marker (diagonal cross)
  x,
}
