import 'package:flutter/material.dart';

/// Customization for major tick marks
class MajorTickLines {
  const MajorTickLines({this.size = 8.0, this.width = 1.0, this.color});

  final double size;
  final double width;
  final Color? color;
}
